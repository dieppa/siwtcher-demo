#Default mode -> local
services:
  product:
    mode: remote
    remoteHost: http://localhost:8082
  other-product:
    mode: remote
    remoteHost: http://localhost:8082
  other-product-2:
    mode: remote
    remoteHost: http://localhost:8082

server:
    port: 8081
